server:
  port: 8080
  tomcat:
    threads:
      max: 800
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/housting
    username: root
    password: q12345
  jackson:
    #配置时间格式化 前端传过来的时候根据这个格式timestamp
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss




